#include "main.h"
/**
 * _abs - returns the absolute value of a number
 *
 * Return: n*-1 for negative number or n otherwise
 */
int _abs(int)
{
	int n;

	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
